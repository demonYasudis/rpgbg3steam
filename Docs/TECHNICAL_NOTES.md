# Техническая основа — WP-00

## Аудит

На старте папка `E:\githubMoney\rpgbg3` была пустой: Unity-проекта, Git-репозитория,
сцен, кода и тестов не существовало. Исходный AGENTS.md скопирован из Downloads;
оригинал в Downloads не изменяется.

- Unity: **6000.2.8f1**, revision `c9992ac36c34`, установленная локально версия.
- Rendering: **Built-in Render Pipeline**, без URP/HDRP. Выбор для минимальной основы;
  окончательное решение о pixel-perfect presentation относится к WP-18.
- Редактор настроен на 2D; сериализация ассетов — Force Text.
- Ввод: существующий в новом проекте **Input Manager** (`activeInputHandler: 0`).
  Обработка игрового ввода пока не написана; пакет нового Input System не установлен.
- Пакеты: встроенные модули Unity; автоматически добавленный Multiplayer Center удалён
  из manifest, поскольку для основы он не нужен.
- Сцена: `Assets/Scenes/TacticalPrototype.unity`, включена в Build Settings.
- Инфраструктуры EditMode/PlayMode unit-тестов пока нет.
- Существующей игровой архитектуры и prefab-зависимостей для сохранения не было.

## Структура и запуск

- `Assets/Scripts/Core/GameBootstrap.cs` — компонент точки входа, сообщение в Console
  создаёт и предоставляет чистую C#-модель Grid в Awake, в Start подключает отображение и ввод.
- `Assets/Editor/PrototypeSetup.cs` — создание сцены через Unity API и проверка структуры.
  Создание намеренно отказывается перезаписывать существующую сцену.
- `Assets/Scenes/` — сцены прототипа.
- `Packages/`, `ProjectSettings/` — настройки, созданные Unity.

Открыть проект в Unity Hub с версией 6000.2.8f1, открыть TacticalPrototype и нажать Play.
Ожидается поле из 144 гексов: наведение мыши подсвечивает клетку, ЛКМ выбирает её.
Координаты показаны сверху. Камера автоматически подстраивается под пропорции окна Game.
Новые модули добавлять только по мере выполнения этапов, логику отделять от отображения.

## Повторная проверка

Закройте редактор этого проекта и выполните в PowerShell:

```powershell
$unityExe = 'C:\Program Files\Unity\Hub\Editor\6000.2.8f1\Editor\Unity.exe'
$check = Start-Process -FilePath $unityExe -WindowStyle Hidden -Wait -PassThru -ArgumentList '-batchmode -nographics -quit -projectPath E:\githubMoney\rpgbg3 -executeMethod GuildTactics.Editor.PrototypeSetup.Validate -logFile E:\githubMoney\rpgbg3\Logs\foundation-check.log'
$check.ExitCode
```

Unity компилирует скрипты и проверяет сохранённую сцену: одну точку входа,
ортографическую MainCamera, отсутствие потерянных скриптов у корневых объектов и Built-in pipeline.
В логе должна появиться строка `WP-00 validation passed` и успешное завершение.
Это проверка структуры сцены, не визуальная проверка и не PlayMode-тест.

## Ограничения

Сборка standalone player пока не выполнялась. Проверки отображения WP-02 описаны ниже.
Unity может записывать служебные предупреждения о licensing/network в headless-логи;
их следует отличать от ошибок компиляции и завершения проверки.
Проверки математики сетки добавлены в WP-01 без дополнительных пакетов (см. ниже).
Git инициализирован локально; коммитов, remote и публикации нет.
`.gitignore` исключает Library, Temp, Logs, UserSettings, сборки и IDE-файлы;
ассеты и их `.meta` должны храниться вместе.

## WP-01 — модель гексагональной сетки

`Assets/Scripts/HexGrid/` содержит чистые C#-типы без Unity API:

- `HexCoordinates`: неизменяемые axial `(q, r)`, равенство/хеш, шесть направлений,
  расстояние с промежуточными `long`, чтобы исключить переполнение при вычитании координат.
  Некорректный индекс направления вызывает исключение; сосед за диапазоном int — OverflowException.
- `HexGrid`: по умолчанию 12×12, размеры задаются в конструкторе. Это axial-параллелограмм,
  а не offset-прямоугольник: `0 <= q < Width`, `0 <= r < Height`.
  Перечисление клеток детерминировано: сначала q, затем r.
- `HexCell`: координаты, Terrain (`Ground`, `HighGround`, `Pit`, `Blocked`), OccupantId.
  Типы местности пока только хранятся, правила проходимости появятся позже.

`TryGetCell` возвращает false/null вне карты, `GetCell` выбрасывает ArgumentOutOfRangeException.
`GetNeighbors` выдаёт только существующие клетки в порядке `+q, +q-r, -r, -q, -q+r, +r`;
для исходной координаты вне карты — пустой набор.
`TryOccupy` и `TryVacate` меняют занятость через модель. ID непустой и уникален в пределах
сетки, чужой ID не освобождает клетку. Отказ не меняет состояние. Это ещё не система движения:
атомарное перемещение и связь с UnitRuntimeState относятся к следующим этапам.

### Проверки WP-01

`Assets/Editor/HexGridChecks.cs` — исполняемые проверки без NUnit/Test Framework:
20 736 пар клеток сверяются с независимым BFS-расстоянием; проверяются границы,
порядок направлений, равенство, переполнение, размеры, типы местности и конфликты занятости.
BFS существует только в проверке, runtime-поиск пути не реализован.

Запуск в редакторе: `Tools > Guild Tactics > Validate Hex Grid`.
Проверка основы открывает TacticalPrototype: сначала сохраните текущую сцену.
Для batch mode в команде выше замените executeMethod на
`GuildTactics.Editor.HexGridChecks.Run`, а имя лога — на `wp01-check.log`.
Ожидаемая строка: `WP-01 checks passed`.

Если основной проект открыт, запускайте batch mode на отдельной копии Assets, Packages,
ProjectSettings. Unity не позволяет одновременно открыть один проект двумя редакторами.
Визуальная проверка не требуется для чистой модели и не заменяется этими проверками.

## WP-02 — отображение и выбор

- `HexLayout.cs` — единственное место преобразования axial ↔ world (XY), cube rounding,
  углов гекса и границ карты. Гексы вершиной вверх, радиус по умолчанию 1.
- `HexGridView.cs` — 144 SpriteRenderer с общим процедурным спрайтом и стандартным
  материалом Unity. Прозрачная маска образует гекс; небольшие промежутки отделяют клетки.
  Созданные Sprite/Texture освобождаются при уничтожении представления.
- `HexGridInteraction.cs` — Input Manager, луч камеры на плоскость XY, наведение,
  единственный постоянный выбор, IMGUI с координатами, кадрирование с местом для HUD.
  Подсветка: голубая при наведении, золотая при выборе, светло-золотая при их совпадении.
  Клик вне карты не сбрасывает выбор; выход курсора/потеря фокуса убирают наведение.
  Промежуток между изображениями относится к ближайшему логическому гексу.
- `GameBootstrap.cs` создаёт представление в Start и передаёт ему существующую модель.
  Камера явно назначена в сохранённой сцене. Объектов сетки до Play в Hierarchy нет.
  Модель WP-01 не менялась. Сценарии движения/боя не добавлены.

### Проверки WP-02

`HexPresentationChecks.ValidateLayout` проверяет 1 875 центров (включая отрицательные
координаты и три радиуса), обе стороны всех рёбер, углы, границы и неверные радиусы.
`HexPresentationChecks.RunBatch` дополнительно запускает проверки WP-00/01, входит
в настоящий Play Mode сохранённой сцены, проверяет созданные объекты и выбирает все
144 клетки через тот же обработчик экранных координат, который использует мышь.
Проверяются сохранение выбора, независимость подсветок, выход за карту/окно и потеря
фокуса, затем камера рендерится в 1280×720 и 640×960. Проверяются положение всех
углов, видимость центров и прозрачные промежутки между гексами.

Запуск на закрытом проекте (или отдельной копии, если редактор открыт):

```powershell
$unityExe = 'C:\Program Files\Unity\Hub\Editor\6000.2.8f1\Editor\Unity.exe'
$check = Start-Process -FilePath $unityExe -WindowStyle Hidden -Wait -PassThru -ArgumentList '-batchmode -projectPath E:\githubMoney\rpgbg3 -executeMethod GuildTactics.Editor.HexPresentationChecks.RunBatch -logFile E:\githubMoney\rpgbg3\Logs\wp02-check.log'
$check.ExitCode
```

Не добавляйте `-quit` (проверка сама завершает редактор после Play Mode) или `-nographics`
(для рендера нужен графический контекст). RunBatch предназначен для отдельного batch-процесса.
Логи и изображения `Logs/wp02-1280x720.png`, `Logs/wp02-640x960.png` исключены из Git.
Это автоматические проверки в Play Mode без NUnit; пакеты не добавлены.
Снимки камеры не содержат IMGUI. Физические клики мышью и standalone build не проверяются
этим сценарием. Для ручной проверки: Play, крайние и внутренние клетки, наведение после
выбора, клик вне карты, изменение размеров Game; сверху должны обновляться координаты.

Результат 2026-09-20: скрипты скомпилированы Unity 6000.2.8f1 на отдельной копии проекта;
WP-00/01, проверки геометрии и автоматический Play Mode прошли. Оба снимка камеры
просмотрены: гексы разделены, выбранная клетка и наведение различимы, поле не обрезано.
В логе есть служебное сообщение лицензирования об отсутствии access token; оно не
помешало компиляции, входу в Play Mode и рендеру. Ошибок C# и исключений проверки нет.
Остаются намеренные ограничения прототипа: временные спрайты и IMGUI; standalone build
и ручной проход с физическим вводом мыши не выполнялись. WP-03 не начинался.

## WP-03 — пути и диапазон движения

- `HexCell.MovementCost`: положительное целое, по умолчанию 1. Стоимость платится за
  вход в клетку; начальная клетка стоит 0. Нулевые и отрицательные значения отклоняются.
- `HexPathfinder.FindReachable(grid, origin, budget)`: Dijkstra в чистом C#, без Unity API.
  `Ground` и `HighGround` проходимы; `Blocked`, `Pit` и занятые назначения непроходимы.
  Только занятая стартовая клетка разрешена: считается, что в ней стоит движущийся юнит.
  Проверка принадлежности/права управления этим юнитом относится к вызывающему коду.
- `HexMovementRange`: снимок минимальных стоимостей `Costs` и восстановление маршрута
  `GetPathTo(destination)`. Включает начало со стоимостью 0; путь содержит начало и конец.
  Недоступная цель возвращает пустой путь. `FindPath` — удобный вызов того же поиска.
  Снимок и возвращаемые пути доступны только для чтения.
- Неверное/непроходимое начало даёт пустой результат. Неверная/недоступная цель — пустой
  путь. `null` вместо модели и отрицательный бюджет — исключения. Нулевой бюджет даёт
  только допустимое начало. Суммы защищены от переполнения; цена выше `int.MaxValue`
  не представима и такой маршрут недоступен даже при максимальном бюджете.
- Равные стоимости разрешаются по порядку первого обнаружения и фиксированному порядку
  соседей. Нет случайности и зависимости от порядка обхода Dictionary.
  Линейный выбор следующей клетки даёт O(V²), достаточно для поля из 144 клеток.
- `HexMovementPreview` связывает событие `HexGridInteraction.SelectionChanged` с поиском.
  Временное начало — выбранная клетка, бюджет по умолчанию 4, задаётся в GameBootstrap.
  `HexGridView.SetReachableCells` отображает зелёную область; выбор и наведение имеют
  приоритет. Уход наведения восстанавливает зелёный цвет; смена выбора удаляет старую область.
  HUD показывает бюджет и количество доступных назначений без стартовой клетки.

Пример использования из C#:

```csharp
var range = HexPathfinder.FindReachable(grid, start, movementBudget);
if (range.Costs.TryGetValue(destination, out int cost))
{
    var path = range.GetPathTo(destination);
    // path[0] == start; реальное перемещение будет реализовано в WP-04.
}
```

### Проверки WP-03

Меню `Tools > Guild Tactics > Validate Hex Pathfinding` запускает проверки алгоритмов:

- 20 736 пар на открытой сетке 12×12, включая углы и края, сверяются с hex distance.
- 20 фиксированных тестовых карт 6×6 со стоимостью 1–5, препятствиями, ямами и занятостью.
  Для каждого начала и бюджетов 0, 3, 8, int.MaxValue результат сверяется с независимым
  Floyd–Warshall. Проверяются стоимость, допустимость каждого шага, отсутствие циклов
  и повторяемость путей (103 680 комбинаций начало/цель/бюджет).
- Дорогой короткий путь против дешёвого обхода, непроходимый коридор, занятое начало/цель,
  нулевой/отрицательный бюджет, границы, карта 1×1, переполнение и неизменяемость снимка.

Для полного запуска используйте команду WP-02 выше, заменив executeMethod на
`GuildTactics.Editor.HexPathfindingChecks.RunBatch`, лог — на `Logs/wp03-check.log`.
Она использует общий batch-runner: компиляция, проверки WP-00/01/02/03, затем настоящий
Play Mode сохранённой сцены. Дополнительно проверяются зелёная область, смена выбора,
приоритет наведения, очистка/восстановление слоя, бюджет, препятствия/занятость/дорогая
клетка после Refresh. Тестовые изменения модели восстанавливаются, сцена не сохраняется.
Снимки `Logs/wp03-1280x720.png` и `Logs/wp03-640x960.png` проверяют рендер и кадрирование.
Пакеты и NUnit не добавлены; результаты в Logs исключены из Git.

### Ограничения и продолжение

Сохранённая сцена остаётся пустой картой Ground: препятствия и занятость задаются в
проверках. Здесь нет редактора местности, героев, перемещения или анимации маршрута.
После изменения модели нужно заново выполнить поиск и вызвать `HexMovementPreview.Refresh()`;
автоматической подписки на изменения клеток пока нет. Результат поиска не является
разрешением на перемещение при изменившемся состоянии карты.
HighGround пока не имеет бонусов, штрафов или особой визуализации.

Результат проверки 2026-09-20: Unity 6000.2.8f1 скомпилировала код на отдельной копии
проекта, все проверки WP-00/01/02/03 и автоматический Play Mode прошли. Оба снимка
камеры просмотрены: зелёная область, золотой выбор и голубое наведение различимы,
карта помещается в кадр. Ошибок C# и исключений проверки нет; служебное сообщение
лицензирования об access token не помешало запуску. Физический ввод мышью и standalone
build в WP-03 не проверялись. WP-04 не начинался.

Файлы WP-03: добавлены `HexPathfinder.cs`, `HexMovementRange.cs`, `HexMovementPreview.cs`
в `Assets/Scripts/HexGrid/`, `Assets/Editor/HexPathfindingChecks.cs` и их `.meta`.
Изменены `HexCell.cs`, `HexGridView.cs`, `HexGridInteraction.cs`, `Core/GameBootstrap.cs`,
`Editor/HexPresentationChecks.cs`, `Scenes/TacticalPrototype.unity`, README, этот документ
и только статус WP-03 в AGENTS.md.

## WP-04 — герои и перемещение игрока

- `UnitDefinition` хранит неизменяемые общие данные класса; четыре определения находятся
  в `HeroDefinitions`. `UnitRuntimeState` отдельно хранит ID экземпляра и текущую клетку.
- `UnitRuntimeState.TryMoveAlong` повторно проверяет весь маршрут, соседство шагов,
  проходимость, занятость и бюджет. Только после успешной проверки `HexGrid.TryMoveOccupant`
  атомарно переносит занятость, после чего позиция runtime-состояния меняется один раз.
- `PlayerUnitController` создаёт Warrior, Rogue, Ranger и Mage в четырёх валидных клетках,
  выбирает героя кликом, рассчитывает его диапазон и принимает только достижимое назначение.
  Перед фиксацией путь рассчитывается заново, поэтому устаревшая подсветка не разрешает ход.
- Логическое перемещение фиксируется перед анимацией и резервирует цель. На время линейной
  интерполяции по каждому гексу новые клики игнорируются; в конце маркер принудительно
  выравнивается по центру логической клетки. Сейчас это не расходует ход — очередь относится к WP-05.
- `UnitView` создаёт временные цветные круглые маркеры без импортируемых ассетов.
  Это grey-box графика, предназначенная для замены на этапе визуального оформления.

`Tools > Guild Tactics > Validate Unit Movement` выполняет чистые проверки definitions/runtime,
четырёх появлений, легальных и запрещённых маршрутов и атомарной занятости. Batch entry point
`GuildTactics.Editor.UnitMovementChecks.RunBatch` дополнительно запускает все проверки WP-00–04,
Play Mode сохранённой сцены и снимки `Logs/wp04-1280x720.png`, `Logs/wp04-640x960.png`.

В среде реализации 2026-09-21 Unity Editor отсутствовал. Чистая C#-модель была успешно
скомпилирована и прошла smoke-проверку через .NET SDK; весь runtime-код также скомпилирован
с минимальными временными заглушками Unity API. `git diff --check` пройден. Unity-зависимые
Editor/Play Mode проверки добавлены, но их необходимо запустить в Unity 6000.2.8f1.
Standalone build и физический ввод мышью не проверялись.

## WP-05 — очередь ходов и состояния действий

Реализация готова к проверке в Unity; статус `[~]` до запуска Unity/Play Mode и проверки HUD.

- `Combat/TurnManager.cs` — чистая C#-модель. Порядок по убыванию Initiative; равенство
  сохраняет порядок переданного списка. В WP-05 инициатива фиксированная, без d20:
  Rogue 4, Ranger 3, Warrior 2, Mage 1. Сервис случайности относится к WP-06.
- Состояния: AwaitTurn → SelectingAction → Moving/ResolvingAction → SelectingAction →
  TurnComplete. Переход к следующему герою выполняется контроллером в следующем Update,
  поэтому повторное завершение одного хода не пропускает героя.
- Очки движения восстанавливаются при начале хода. Можно пройти несколько маршрутов,
  пока хватает остатка; списывается стоимость входа в клетки. Невалидный маршрут ничего
  не расходует. Действие доступно один раз за ход, независимо от движения.
- Команды игрока проходят через TurnManager; чужой ход, повторное действие и команды
  во время анимации отклоняются. `UnitRuntimeState.TryMoveAlong` остаётся низкоуровневой
  операцией проверки/фиксации маршрута: новые игровые команды должны использовать TurnManager.
- Временная кнопка Wait расходует действие и показывает ResolvingAction 0,35 секунды.
  Это проверка цикла, а не атака. End turn позволяет отказаться от оставшихся ресурсов.
- PlayerUnitController восстанавливает подсветку после включения. При отключении во время
  движения маркер выравнивается с уже зафиксированной целью; потраченные ресурсы не возвращаются.
  Прерванное ожидание считается использованным действием. Анимации используют unscaledDeltaTime.
- TurnOrderUI создаётся из GameBootstrap. HUD занимает верхние 204 пикселя; клики в этой
  области не отправляются на поле. Сцена, сериализованные поля и зависимости не менялись.

### Проверки и запуск

`Tools > Guild Tactics > Validate Turns` запускает проверки модели. Проверяются стабильность
при равной/крайней инициативе, 100 переходов, одиночный неподвижный герой, пустой/повторный
список, чужие команды, взвешенное и составное движение, расход/сброс ресурсов и блокировки.

Batch entry point: `GuildTactics.Editor.TurnChecks.RunBatch`. Он запускает прежние проверки
и Play Mode сохранённой сцены, затем проверяет завершение анимаций, блокировки повторного
ввода, смену героя, наличие UI и восстановление после отключения контроллера.
Сначала сохраните сцену и закройте редактор данного проекта. Запуск из корня проекта:

```powershell
$unityExe = 'C:\Program Files\Unity\Hub\Editor\6000.2.8f1\Editor\Unity.exe'
$projectPath = (Get-Location).Path
& $unityExe -batchmode -projectPath $projectPath -executeMethod GuildTactics.Editor.TurnChecks.RunBatch -logFile "$projectPath\Logs\wp05-check.log"
```

Не добавлять `-quit` и `-nographics`: сценарий сам завершает процесс и проверяет рендер.
Для ручной проверки: Play; очередь видна, выбирается Rogue; движение расходует очки;
Wait доступен один раз; во время обеих анимаций клики и End turn заблокированы;
после четырёх End turn начинается раунд 2 с восстановленными ресурсами.
Проверить HUD в 1280×720 и 640×960: снимки камеры не включают IMGUI.

Результат 2026-09-22: Unity Editor в среде отсутствует. Через Roslyn/.NET выполнены чистые
проверки WP-01/03/04/05; все прошли. Проверка сцены из WP-01 при этом не выполнялась.
Весь runtime и файлы `*Checks.cs` компилируются с временными заглушками Unity API при
C# 9. Это не компиляция против настоящих Unity assemblies и не Play Mode. Два предупреждения
этого прогона относятся к назначаемой в сцене камере и неиспользуемому событию заглушки.
Временные файлы проверок находятся в игнорируемой папке `Logs/WP05Checks`.

Добавлены: TurnManager.cs, TurnOrderUI.cs, TurnChecks.cs, их .meta и Combat.meta.
Изменены: UnitDefinition.cs, HeroDefinitions.cs, PlayerUnitController.cs, GameBootstrap.cs,
HexGridInteraction.cs, UnitMovementChecks.cs, HexPresentationChecks.cs, README.md,
AGENTS.md и этот документ. WP-06 не начинался.

Остаётся: запустить Unity batch-проверку и визуально проверить HUD/управление. До этого
нельзя подтвердить интеграцию с реальным Unity runtime. Standalone build не проверялся.
Технический долг: временный IMGUI и тестовое действие Wait; новых production-пакетов нет.

## WP-06 — кубики и базовые атаки

Реализация готова к проверке в Unity, статус `[~]`. WP-05 сохраняет прежний статус;
WP-07 не начинался.

- `Combat/Dice.cs`: интерфейс `IDice.Roll(sides)` и отдельный поток `SeededDice` на основе
  `System.Random`. Диапазон — 1..sides, включая обе границы. Seed сохраняется в экземпляре,
  задаётся в `GameBootstrap` (по умолчанию 12345), сериализован в сцене, виден в HUD и Console.
  Одинаковый seed и порядок допустимых атак повторяют броски в одной версии runtime;
  совместимость последовательности между разными реализациями .NET не обещается.
- `UnitDefinition` получил неизменяемые MaxHealth, Attack, Defense, DamageDie, DamageBonus.
  Новые аргументы конструктора необязательные, прежние вызовы сохранены. Для прототипа
  у всех героев пока 20 HP, Attack 4, Defense 12, d6+2; различия классов относятся к WP-08.
- `UnitRuntimeState`: собственное CurrentHealth, IsAlive и команда Player/Enemy.
  Урон не меняет общие определения. При HP=0 занятость снимается немедленно, движение
  погибшего запрещается. Состояние привязано к своей карте, одинаковый ID на другой карте
  не даёт права атаковать или перемещать чужого юнита.
- `CombatSystem`: единственная проверка цели — активный живой атакующий со свободным
  действием, живой противник другой команды на этой карте, расстояние ровно один гекс.
  Недопустимые попытки не расходуют действие и случайные числа. Успешная команда сразу
  переводит ход в ResolvingAction и применяет результат один раз, до показа анимации.
  Повторные клики, движение и End turn блокируются до завершения обратной связи.
- Формула: `d20 + Attack >= Defense`; при попадании `max(0, DamageDie + DamageBonus)`.
  Нет автоматических попаданий/промахов на 20/1 и нет критических ударов. Промах тратит
  действие, но не бросает кубик урона. Суммы вычисляются через long; урон ограничен int.MaxValue,
  HP ограничено нулём. AttackResult хранит отдельно рассчитанный и фактически снятый урон.
  При некорректном/бросающем исключение IDice HP не меняется, действие остаётся потраченным,
  блокировка разрешения снимается и исключение передаётся вызывающему коду.
- `PlayerUnitController` сохраняет Units как список четырёх героев; Enemies содержит
  одного Crypt Sentinel на (1,3), 18 HP / Defense 12. Он неподвижен и не входит в очередь:
  это тестовая цель WP-06, ИИ/вражеские ходы/победа и поражение относятся к WP-07.
  ЛКМ по соседнему врагу атакует, по свободной зелёной клетке — перемещает.
  Публичный TryWaitAction сохранён как пропуск действия и для регрессионных проверок WP-05.
- `CombatText`: всплывающий результат броска и урона на 1,2 секунды, переживает скрытие
  погибшего юнита. `UnitView` показывает HP, погибшее представление выключается.
  Прерывание показа отключением контроллера снимает блокировку, не возвращает действие
  и не отменяет/повторяет урон. `TurnManager` пропускает погибших; пустая живая очередь
  возвращает false без зацикливания. Это не экран окончания боя.

### Проверки WP-06

`Assets/Editor/CombatChecks.cs` проверяет 6000 пар повторяемых бросков для d1/d4/d6/d8/d20
и int.MaxValue, границы формулы, отсутствие скрытых правил 1/20, промах без броска урона,
отрицательный бонус, переполнение, неверные команды/цели/карты, повторные команды,
блокировку движения и завершения хода, HP/смерть/освобождение клетки, сохранность определения,
пропуск погибших в очереди, пустую живую очередь и ошибки IDice.

Меню: `Tools > Guild Tactics > Validate Basic Combat` (проверки модели).
Полный batch-runner дополнен отдельным Play Mode сценарием с подставными бросками:
клик мыши по врагу, всплывающий результат, блокировка повторов, прерывание показа,
смерть после трёх попаданий, скрытие представления и движение в освободившуюся клетку.
Старые проверки сцены учитывают пятого юнита, сохраняя требование ровно четырёх героев.

Запуск на машине с Unity 6000.2.8f1, при закрытом редакторе этого проекта:

```powershell
$unityExe = 'C:\Program Files\Unity\Hub\Editor\6000.2.8f1\Editor\Unity.exe'
$projectPath = (Get-Location).Path
$logPath = Join-Path $projectPath 'Logs\wp06-unity-check.log'
New-Item -ItemType Directory -Path (Join-Path $projectPath 'Logs') -Force | Out-Null
$unityArguments = '-batchmode -projectPath "{0}" -executeMethod GuildTactics.Editor.CombatChecks.RunBatch -logFile "{1}"' -f $projectPath, $logPath
$check = Start-Process -FilePath $unityExe -WindowStyle Hidden -Wait -PassThru -ArgumentList $unityArguments
$check.ExitCode
```

Не добавляйте `-quit` или `-nographics`: runner сам завершает редактор и проверяет рендер.
Он открывает TacticalPrototype и не сохраняет тестовые изменения в сцене.
Физические клики и IMGUI проверяются вручную: Play → атака Rogue по оранжевому врагу →
повторные клики/End turn во время результата → смена героев → следующие атаки до смерти →
движение на освобождённую клетку. Проверить читаемость HP/результата в 1280×720 и 640×960,
повторяемость после перезапуска и изменение бросков при смене seed. Камерные PNG не включают IMGUI.

Результат 2026-09-22: Unity Editor отсутствует. Весь runtime и все `*Checks.cs` прошли
компиляционную проверку Roslyn C# 9 с временными заглушками Unity API. Выполнены чистые
модельные проверки WP-01/03/04/05/06; все прошли. Проверка Unity-сцены из WP-01 явно пропущена.
Это не компиляция против Unity assemblies, не Play Mode и не визуальная проверка.
Временный стенд расположен в игнорируемой `Logs/WP06Checks`; предупреждения относятся
к назначаемой в сцене камере и неиспользуемому событию заглушки.

Остаётся запустить полный Unity batch-runner и ручную проверку IMGUI. Standalone build
не проверялся. Технический долг ограничен временным IMGUI, едиными боевыми параметрами
героев и неподвижной тестовой целью. Новых пакетов/production-зависимостей нет.

### Изменённые файлы WP-06

Добавлены (каждый со своим `.meta`):

- `Assets/Scripts/Combat/Dice.cs`
- `Assets/Scripts/Combat/AttackResult.cs`
- `Assets/Scripts/Combat/CombatSystem.cs`
- `Assets/Scripts/Combat/CombatText.cs`
- `Assets/Editor/CombatChecks.cs`

Изменены:

- `Assets/Scripts/Units/UnitDefinition.cs`
- `Assets/Scripts/Units/UnitRuntimeState.cs`
- `Assets/Scripts/Units/PlayerUnitController.cs`
- `Assets/Scripts/Units/UnitView.cs`
- `Assets/Scripts/Combat/TurnManager.cs`
- `Assets/Scripts/Combat/TurnOrderUI.cs`
- `Assets/Scripts/Core/GameBootstrap.cs`
- `Assets/Scripts/HexGrid/HexGridInteraction.cs`
- `Assets/Editor/HexPresentationChecks.cs`
- `Assets/Editor/UnitMovementChecks.cs`
- `Assets/Scenes/TacticalPrototype.unity`
- `README.md`
- `Docs/TECHNICAL_NOTES.md`
- `AGENTS.md` (только статус WP-06)

`git diff --check` прошёл; все C#-ассеты имеют `.meta`, GUID не дублируются.


## WP-07 — враги и ближний ИИ (2026-09-22)

Реализованы три врага и общая стабильная очередь инициативы: четыре героя,
Crypt Sentinel (инициатива 3) и два Crypt Guard (инициатива 2).
Противники сначала атакуют соседнего героя, иначе выбирают кратчайший по стоимости
доступный путь к клетке атаки, проходят доступную часть пути и атакуют после подхода.
Равные маршруты разрешаются детерминированно. При недоступных целях или нулевом
движении ход завершается; повторного планирования в том же ходу нет.
До WP-10 все живые герои считаются видимыми. На исходной карте препятствий нет;
обход препятствий проверяется на отдельной тестовой карте.

MeleeBrain и BattleRules — чистая C#-логика. PlayerUnitController использует общие
системы движения, урона и анимации для обеих команд. Публичные команды игрока
блокируются во вражеский ход. Отключение компонента завершает уже применённую
операцию без повторного списания ресурсов; ИИ продолжает после включения.
После последней анимации выводится VICTORY / DEFEAT, очередь останавливается,
подсветка очищается. При одновременном отсутствии обеих сторон выбирается Defeat.
Для повторного боя пока нужен новый Play; экран перезапуска относится к WP-17.

Проверки:
- Roslyn C# 9: runtime и все редакторские *Checks.cs с временными заглушками Unity API.
- Пройдены модельные проверки WP-01/03/04/05/06/07. Новый сценарий проверяет стену
  с обходом, полностью закрытый проход, выбор другой доступной цели, погибших героев,
  нулевой и взвешенный бюджет, повторяемость, полный бой до поражения и победу.
- В общий batch-runner добавлен Play Mode сценарий: семь участников, атака врага,
  запрет команд игрока, отключение во время атаки и движения, победа/поражение.
- Unity Editor на этой машине отсутствует: Play Mode сценарий, визуальная проверка
  интерфейса и компиляция против настоящих Unity assemblies НЕ выполнялись.
  WP-07 поэтому отмечен [~], WP-08 не начат.

Для полной проверки на машине с Unity используйте команду WP-06 выше с
`-executeMethod GuildTactics.Editor.EnemyChecks.RunBatch` и отдельным логом WP-07.
Не добавляйте -quit/-nographics. Вручную: Play, завершить ходы Rogue/Ranger,
дождаться атаки Sentinel и подхода Guard; попытаться кликать во время хода врага;
проверить окончание боя и читаемость очереди на широком и узком окне.

Добавлены: Assets/Scripts/Combat/MeleeBrain.cs, BattleOutcome.cs,
Assets/Editor/EnemyChecks.cs (и три .meta).
Изменены: PlayerUnitController.cs, TurnOrderUI.cs, GameBootstrap.cs,
HexGridInteraction.cs, CombatChecks.cs, UnitMovementChecks.cs,
HexPresentationChecks.cs, README.md, AGENTS.md и этот документ.
Serialized-поля и сцена в этом пакете не изменялись, новых пакетов нет.
Технический долг: временный IMGUI, фиксированный состав врагов, отсутствие тумана
войны и ручной перезапуск. Существующие незакоммиченные изменения сохранены.
